# `app/api/`

Route handlers: refresh trigger, `/api/health`, Auth.js callback routes.

Populated across sub-plans 07-09:
- `plans/scaffold/08-auth-admin.md` — "Refresh now" route handler.
- `plans/scaffold/09-public-and-health.md` — `GET /api/health`.
